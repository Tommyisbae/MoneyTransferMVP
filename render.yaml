services:
     - type: web
       name: money-transfer-mvp
       env: python
       plan: free
       buildCommand: |
         apt-get update && apt-get install -y tesseract-ocr libtesseract-dev
         pip install -r requirements.txt
       startCommand: gunicorn whatsapp_money_transfer_mvp:app